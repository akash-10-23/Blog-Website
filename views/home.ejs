<%- include("partials/header"); %> 

    <h1>Home</h1>

    <p> <%= homeText %>  </p>

    <% Posts.forEach(post => { %>
        
        <h2> <%= post.title %> </h2>
        <p> <%= post.postDetail.substring(0,100)+ "..." %> <a href="/post/<%= post.title %> ">Read More</a></p> 

    <% }) %>
<%- include("partials/footer"); %> 